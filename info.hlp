# ---------
# info help
# ---------

------------------------
DotMan                .
A Manager for .NET   /|\
v0.1.1               / \
------------------------

https://github.com/reallukee/dotman

Description:
  DotMan Info Utility

Usage:
  dotman info [target] [options]
  info [target] [options]

Commands:
  sdk
  runtime
  netCoreRuntime
  desktopCoreRuntime
  aspNetCoreRuntime

Options:
  -help
  -version
  -online
  -channels
  -channel [...]
    -runtime [...]
  -filter [all|releases|rcs|previews]
  -latest
  -nocache

Examples:
  dotman info sdk -online -channel 8.0 -runtime 8.0.10
  dotman info sdk -online -channel 8.0
  dotman info sdk -online -channels
  dotman info sdk -online
  dotman info runtime -online -channel 8.0 -runtime 8.0.10
  dotman info runtime -online -channel 8.0
  dotman info runtime -online -channels
  dotman info runtime -online

