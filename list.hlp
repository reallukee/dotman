# ---------
# list help
# ---------

------------------------
DotMan                .
A Manager for .NET   /|\
vA.B.C               / \
------------------------

https://github.com/reallukee/dotman

Description:
  DotMan List Utility

Usage:
  dotman list [target] [options]
  list [target] [options]

Commands:
  sdk
  runtime
  netCoreRuntime
  desktopCoreRuntime
  aspNetCoreRuntime

Options:
  -help
  -version
  -online
  -channels
  -channel [...]
    -runtime [...]
    -runtimes
  -filter [all|releases|rcs|previews]
  -platform [all|current]
  -latest
  -noCache

Examples:
  dotman list sdk -online -channel 8.0 -runtime 8.0.10
  dotman list sdk -online -channel 8.0
  dotman list sdk -online
  dotman list sdk -online -channels
  dotman list sdk -online -runtimes
  dotman list runtime -online -channel 8.0 -runtime 8.0.10
  dotman list runtime -online -channel 8.0
  dotman list runtime -online
  dotman list runtime -online -channels
  dotman list runtime -online -runtimes

